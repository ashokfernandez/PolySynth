# Cppcheck suppressions for PolySynth.
# Format: [error-id]:[file-glob]  or  [error-id]
# Lines starting with # are comments. No blank lines between entries.
# 3rd-party code – not our responsibility to fix
*:*/external/catch2/*
*:*/nlohmann/*
*:*/external/daisysp/*
*:*/daisysp/*
*:*/iPlug2/*
*:*/external/iPlug2/*
# System-header noise when running without full sysroot
missingIncludeSystem
# Prevents CI failure after removing code that had a suppression entry
unmatchedSuppression
# DSP float arithmetic is intentionally lossy in these files
floatConversion:*/sea_dsp/*
floatConversion:*/SEA_DSP/*
# --- Pre-existing issues baselined on hardening-pipeline PR ---
# src/core: style issues in Engine.h and VoiceManager.h
functionStatic:*/src/core/Engine.h
useStlAlgorithm:*/src/core/VoiceManager.h
# libs/SEA_Util: functionConst on template instantiations of VoiceAllocator
functionConst:*/libs/SEA_Util/*
# tests/mocks: intentional mock stubs
noExplicitConstructor:*/tests/mocks/*
functionStatic:*/tests/mocks/*
# tests/unit: pre-existing issues across test files
unreadVariable:*/tests/unit/*
variableScope:*/tests/unit/*
constVariable:*/tests/unit/*
constVariable:*/tests/demos/*
comparisonOfFuncReturningBoolError:*/tests/unit/*
useStlAlgorithm:*/tests/unit/*
truncLongCastAssignment:*/tests/unit/*
suspiciousFloatingPointCast:*/tests/unit/*
# tests/utils: intentional C-style casts in WAV writer for binary serialisation
cstyleCast:*/tests/utils/*
dangerousTypeCast:*/tests/utils/*
